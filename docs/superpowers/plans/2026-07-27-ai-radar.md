# AI Radar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 本地定时管道：监控主流 AI 公司发布博客 → kimi 大白话解读 → 飞书卡片推送 → 静态站生成。

**Architecture:** Python 单目录项目。`fetch.py`（抓取+去重+SQLite）→ `interpret.py`（kimi CLI 日常 / Moonshot API 深度）→ `push_lark.py`（飞书卡片）→ `build_site.py`（静态 HTML），`main.py` 串联，launchd 每天 08:00/20:00 调度，周日加跑 `--weekly` 汇总。

**Tech Stack:** Python 3.11+、feedparser、PyYAML、requests、beautifulsoup4、pytest、SQLite（stdlib）、`kimi -p` 非交互模式（输出到 stdout，已核实官方文档）、飞书自定义机器人 webhook。

**Spec:** `docs/superpowers/specs/2026-07-27-ai-radar-design.md`

---

### Task 1: 项目脚手架

**Files:**
- Create: `requirements.txt`
- Create: `.gitignore`
- Create: `tests/__init__.py`（空文件）
- Create: `tests/test_smoke.py`

- [ ] **Step 1: 创建依赖与忽略文件**

`requirements.txt`:
```
feedparser==6.0.11
PyYAML==6.0.2
requests==2.32.3
beautifulsoup4==4.12.3
pytest==8.3.4
```

`.gitignore`:
```
.venv/
__pycache__/
data/
*.pyc
```

`tests/__init__.py`：空文件。

- [ ] **Step 2: 写冒烟测试**

`tests/test_smoke.py`:
```python
def test_sanity():
    assert True
```

- [ ] **Step 3: 建 venv 并跑测试**

```bash
cd /Users/bytedance/ai-radar
python3 -m venv .venv
.venv/bin/pip install -q -r requirements.txt
.venv/bin/pytest tests/ -v
```
Expected: 1 passed

- [ ] **Step 4: Commit**

```bash
git add requirements.txt .gitignore tests/
git commit -m "chore: 项目脚手架"
```

---

### Task 2: sources.yaml 与配置加载

**Files:**
- Create: `sources.yaml`
- Create: `fetch.py`（仅 load_sources）
- Test: `tests/test_fetch.py`

- [ ] **Step 1: 写失败测试**

`tests/test_fetch.py`:
```python
import fetch


def test_load_sources(tmp_path):
    p = tmp_path / "sources.yaml"
    p.write_text(
        "sources:\n"
        "  - name: OpenAI\n"
        "    type: rss\n"
        "    url: https://example.com/rss.xml\n"
        "    keywords: [gpt, release]\n",
        encoding="utf-8")
    sources = fetch.load_sources(str(p))
    assert sources[0]["name"] == "OpenAI"
    assert sources[0]["type"] == "rss"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_fetch.py -v`
Expected: FAIL，`ModuleNotFoundError: No module named 'fetch'`

- [ ] **Step 3: 实现 load_sources**

`fetch.py`:
```python
import hashlib
import sqlite3
import time

import feedparser
import requests
import yaml
from bs4 import BeautifulSoup

USER_AGENT = {"User-Agent": "ai-radar/0.1"}


def load_sources(path="sources.yaml"):
    with open(path, encoding="utf-8") as f:
        return yaml.safe_load(f)["sources"]
```

- [ ] **Step 4: 写 sources.yaml（候选源，Task 6 验证后定稿）**

`sources.yaml`:
```yaml
sources:
  - name: OpenAI
    type: rss
    url: https://openai.com/news/rss.xml
    keywords: [model, gpt, release, introducing]
  - name: Google DeepMind
    type: rss
    url: https://deepmind.google/blog/rss.xml
    keywords: [model, gemini, release]
  - name: Anthropic
    type: scrape
    url: https://www.anthropic.com/news
    selector: "a[href^='/news/']"
    base: https://www.anthropic.com
    keywords: [claude, model, release]
```

- [ ] **Step 5: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_fetch.py -v`
Expected: 1 passed

- [ ] **Step 6: Commit**

```bash
git add sources.yaml fetch.py tests/test_fetch.py
git commit -m "feat: 源配置加载"
```

---

### Task 3: SQLite 存储与去重

**Files:**
- Modify: `fetch.py`
- Test: `tests/test_fetch.py`

- [ ] **Step 1: 追加失败测试**

`tests/test_fetch.py` 追加：
```python
def test_save_new_dedup(tmp_path):
    conn = fetch.init_db(str(tmp_path / "t.db"))
    entry = {"source": "OpenAI", "url": "https://x.com/a",
             "title": "GPT-X 发布", "summary": "", "published": ""}
    first = fetch.save_new(conn, entry)
    second = fetch.save_new(conn, entry)
    assert first is not None
    assert second is None


def test_passes_keywords():
    src = {"keywords": ["gpt", "release"]}
    assert fetch.passes_keywords(src, "Introducing GPT-5", "")
    assert not fetch.passes_keywords(src, "Our hiring update", "")
    assert fetch.passes_keywords({"name": "NoKw"}, "anything", "")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_fetch.py -v`
Expected: FAIL，`AttributeError: module 'fetch' has no attribute 'init_db'`

- [ ] **Step 3: 实现存储与去重**

`fetch.py` 追加：
```python
SCHEMA = """
CREATE TABLE IF NOT EXISTS articles (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    source TEXT NOT NULL,
    url TEXT NOT NULL UNIQUE,
    title TEXT NOT NULL,
    title_hash TEXT NOT NULL,
    summary TEXT DEFAULT '',
    published TEXT DEFAULT '',
    status TEXT NOT NULL DEFAULT 'new',
    importance INTEGER,
    interpretation TEXT,
    fetched_at TEXT NOT NULL DEFAULT (datetime('now')),
    pushed_at TEXT
);
"""


def init_db(db_path="data/radar.db"):
    conn = sqlite3.connect(db_path)
    conn.executescript(SCHEMA)
    return conn


def title_hash(title):
    return hashlib.sha1(title.strip().lower().encode()).hexdigest()


def passes_keywords(source, title, summary):
    kws = source.get("keywords")
    if not kws:
        return True
    text = (title + " " + (summary or "")).lower()
    return any(k.lower() in text for k in kws)


def save_new(conn, entry):
    """url 已存在则返回 None，否则插入并返回 row id。"""
    if not entry["url"] or not entry["title"]:
        return None
    if conn.execute("SELECT 1 FROM articles WHERE url = ?",
                    (entry["url"],)).fetchone():
        return None
    cur = conn.execute(
        "INSERT INTO articles (source, url, title, title_hash, summary, published)"
        " VALUES (?, ?, ?, ?, ?, ?)",
        (entry["source"], entry["url"], entry["title"],
         title_hash(entry["title"]), entry["summary"], entry["published"]))
    return cur.lastrowid
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_fetch.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add fetch.py tests/test_fetch.py
git commit -m "feat: SQLite 存储与 url 去重"
```

---

### Task 4: RSS 抓取

**Files:**
- Modify: `fetch.py`
- Create: `tests/fixtures/rss.xml`
- Test: `tests/test_fetch.py`

- [ ] **Step 1: 写 fixture**

`tests/fixtures/rss.xml`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"><channel><title>T</title>
<item><title>Introducing GPT-X</title>
<link>https://example.com/gpt-x</link>
<description>A new model.</description>
<pubDate>Mon, 27 Jul 2026 08:00:00 GMT</pubDate></item>
</channel></rss>
```

- [ ] **Step 2: 写失败测试**

`tests/test_fetch.py` 追加：
```python
def test_fetch_entries_rss():
    source = {"name": "OpenAI", "type": "rss",
              "url": "tests/fixtures/rss.xml"}
    entries = fetch.fetch_entries(source)
    assert entries[0]["title"] == "Introducing GPT-X"
    assert entries[0]["url"] == "https://example.com/gpt-x"
    assert entries[0]["source"] == "OpenAI"
```

- [ ] **Step 3: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_fetch.py -v`
Expected: FAIL，`AttributeError: ... 'fetch_entries'`

- [ ] **Step 4: 实现 RSS 分支**

`fetch.py` 追加：
```python
def fetch_entries(source, timeout=20):
    """返回 [{source, url, title, summary, published}]。"""
    if source["type"] == "rss":
        feed = feedparser.parse(source["url"])
        return [{
            "source": source["name"],
            "url": e.get("link", ""),
            "title": e.get("title", "").strip(),
            "summary": e.get("summary", ""),
            "published": e.get("published", ""),
        } for e in feed.entries]
    raise ValueError(f"unknown source type: {source['type']}")
```

- [ ] **Step 5: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_fetch.py -v`
Expected: 4 passed

- [ ] **Step 6: Commit**

```bash
git add fetch.py tests/fixtures/rss.xml tests/test_fetch.py
git commit -m "feat: RSS 抓取"
```

---

### Task 5: 网页抓取与正文提取

**Files:**
- Modify: `fetch.py`
- Create: `tests/fixtures/page.html`
- Test: `tests/test_fetch.py`

- [ ] **Step 1: 写 fixture**

`tests/fixtures/page.html`:
```html
<html><body>
<a href="/news/claude-y">Claude Y Released</a>
<article><p>Full article body here.</p></article>
</body></html>
```

- [ ] **Step 2: 写失败测试**

`tests/test_fetch.py` 追加（用 `requests.get` mock 读本地 fixture）：
```python
from unittest.mock import patch


class FakeResp:
    def __init__(self, text):
        self.text = text
        self.ok = True

    def raise_for_status(self):
        pass


def load_fixture(name):
    with open(f"tests/fixtures/{name}", encoding="utf-8") as f:
        return f.read()


def test_fetch_entries_scrape():
    source = {"name": "Anthropic", "type": "scrape",
              "url": "https://www.anthropic.com/news",
              "selector": "a[href^='/news/']",
              "base": "https://www.anthropic.com"}
    with patch("fetch.requests.get", return_value=FakeResp(load_fixture("page.html"))):
        entries = fetch.fetch_entries(source)
    assert entries[0]["url"] == "https://www.anthropic.com/news/claude-y"
    assert entries[0]["title"] == "Claude Y Released"


def test_fetch_article_text():
    with patch("fetch.requests.get", return_value=FakeResp(load_fixture("page.html"))):
        text = fetch.fetch_article_text("https://x.com/a")
    assert "Full article body here." in text
```

- [ ] **Step 3: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_fetch.py -v`
Expected: FAIL（scrape 分支抛 ValueError / 无 fetch_article_text）

- [ ] **Step 4: 实现 scrape 分支与正文提取**

`fetch.py` 中把 `fetch_entries` 末尾的 `raise ValueError` 替换为：
```python
    resp = requests.get(source["url"], timeout=timeout, headers=USER_AGENT)
    resp.raise_for_status()
    soup = BeautifulSoup(resp.text, "html.parser")
    entries = []
    for a in soup.select(source["selector"]):
        href = a.get("href", "")
        if href.startswith("/"):
            href = source["base"] + href
        entries.append({"source": source["name"], "url": href,
                        "title": a.get_text(strip=True),
                        "summary": "", "published": ""})
    return entries
```

`fetch.py` 追加：
```python
def fetch_article_text(url, timeout=20, max_chars=8000):
    resp = requests.get(url, timeout=timeout, headers=USER_AGENT)
    resp.raise_for_status()
    soup = BeautifulSoup(resp.text, "html.parser")
    root = soup.find("article") or soup.body or soup
    return root.get_text("\n", strip=True)[:max_chars]
```

- [ ] **Step 5: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_fetch.py -v`
Expected: 6 passed

- [ ] **Step 6: 实现 collect() 并补集成测试**

`fetch.py` 追加：
```python
def collect(db_path="data/radar.db", sources_path="sources.yaml"):
    """抓全部源，存关键词命中的新条目，返回新 row id 列表。单源失败只记日志。"""
    conn = init_db(db_path)
    new_ids = []
    for source in load_sources(sources_path):
        try:
            entries = fetch_entries(source)
        except Exception as e:
            print(f"[fetch] {source['name']} failed: {e}")
            continue
        for entry in entries:
            if not passes_keywords(source, entry["title"], entry["summary"]):
                continue
            row_id = save_new(conn, entry)
            if row_id:
                new_ids.append(row_id)
        conn.commit()
        time.sleep(1)
    conn.close()
    return new_ids
```

`tests/test_fetch.py` 追加：
```python
def test_collect(tmp_path):
    p = tmp_path / "sources.yaml"
    p.write_text(
        "sources:\n"
        "  - name: OpenAI\n"
        "    type: rss\n"
        "    url: tests/fixtures/rss.xml\n"
        "    keywords: [gpt]\n",
        encoding="utf-8")
    with patch("fetch.time.sleep"):
        ids = fetch.collect(str(tmp_path / "t.db"), str(p))
    assert len(ids) == 1
```

- [ ] **Step 7: 跑全部测试**

Run: `.venv/bin/pytest tests/ -v`
Expected: 8 passed

- [ ] **Step 8: Commit**

```bash
git add fetch.py tests/
git commit -m "feat: 网页抓取、正文提取与 collect 管道"
```

---

### Task 6: 验证并定稿 sources.yaml

**Files:**
- Modify: `sources.yaml`

说明：各厂博客的 RSS 可用性会变化，必须实测，不许凭记忆写死。

- [ ] **Step 1: 实测 RSS 候选**

```bash
for u in \
  "https://openai.com/news/rss.xml" \
  "https://deepmind.google/blog/rss.xml" \
  "https://www.anthropic.com/rss.xml" \
  "https://mistral.ai/news/rss.xml" \
  "https://qwen.ai/rss.xml" ; do
  code=$(curl -s -o /dev/null -w "%{http_code}" -L --max-time 15 "$u")
  echo "$code $u"
done
```
Expected: 输出每个 URL 的状态码。200 且内容确为 RSS/Atom 的保留为 `type: rss`；404/非 XML 的改为 `type: scrape`。

- [ ] **Step 2: 对 scrape 源实测 selector**

对每个 scrape 源：`curl -s <列表页> | grep -oE 'href="[^"]*"' | head -30`，找出能选中文章链接的 CSS selector，写入配置。抓不到链接的源（纯 JS 渲染）在本版移除并在 README 记录原因。

- [ ] **Step 3: 覆盖 spec 要求的 15 家**

RSS 可用：OpenAI、Anthropic、Google DeepMind、Mistral、Cohere、Moonshot、智谱、Qwen 中能实测通过的；其余（Meta AI、xAI、DeepSeek、字节 Seed、阶跃、MiniMax、百度文心等）配 scrape 或按 Step 2 结果移除。最终 `sources.yaml` 每条必须有 `name/type/url`，scrape 必须有 `selector/base`。

- [ ] **Step 4: 全量实跑一次 collect 验证**

```bash
.venv/bin/python -c "import fetch; ids = fetch.collect(); print(len(ids), 'new entries')"
```
Expected: 无异常 traceback（单源失败打印 `[fetch] ... failed:` 可接受，但失败源要在 sources.yaml 里修或删）。

- [ ] **Step 5: Commit**

```bash
git add sources.yaml
git commit -m "chore: 实测定稿监控源清单"
```

---

### Task 7: 解读 prompt

**Files:**
- Create: `prompt.md`

- [ ] **Step 1: 写 prompt.md**

```markdown
你是 AI Radar 的解读编辑。读者是没有技术背景、但关心 AI 产品的人。

下面是 {source} 的一篇官方博客文章。

标题：{title}
链接：{url}
正文（可能截断）：
{content}

任务：判断这篇文章是否与"AI 模型或重要产品发布"相关，并输出解读。

硬性要求：
- "一句话"禁止使用任何技术术语；非用不可时，当场用大白话解释。
- "value" 必须分三类读者各写一句：普通用户 / 开发者（API 价格与能力）/ 关注行业的人。
- "comparison" 用一句话定位这次发布相对其他公司同类能力的水平。
- importance：5=旗舰级新模型，4=重要能力升级，3=常规更新，2=小改动，1=几乎无关。
- credibility：官方公告填 "official"，第三方爆料填 "third-party"。
- 只输出一个 JSON 对象，不要输出任何其他文字、不要用 markdown 代码块包裹。

输出格式：
{"relevant": true或false, "importance": 1到5的整数, "one_liner": "...", "value": "...", "comparison": "...", "credibility": "official"}
```

- [ ] **Step 2: Commit**

```bash
git add prompt.md
git commit -m "feat: 解读 prompt 初版"
```

---

### Task 8: interpret.py 解析逻辑

**Files:**
- Create: `interpret.py`
- Test: `tests/test_interpret.py`

- [ ] **Step 1: 写失败测试**

`tests/test_interpret.py`:
```python
import pytest

import interpret


TEMPLATE = "源:{source} 题:{title} 链:{url} 文:{content}"


def test_build_prompt():
    article = {"source": "OpenAI", "title": "GPT-X", "url": "https://x.com/a"}
    out = interpret.build_prompt(TEMPLATE, article, "正文内容")
    assert "源:OpenAI" in out and "正文内容" in out


def test_parse_interpretation_ok():
    text = '前言 {"relevant": true, "importance": 4, "one_liner": "a", "value": "b", "comparison": "c", "credibility": "official"} 后记'
    data = interpret.parse_interpretation(text)
    assert data["importance"] == 4


def test_parse_interpretation_bad():
    with pytest.raises(ValueError):
        interpret.parse_interpretation("no json here")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_interpret.py -v`
Expected: FAIL，`ModuleNotFoundError`

- [ ] **Step 3: 实现**

`interpret.py`:
```python
import json
import os
import re
import subprocess

import requests

PROMPT_PATH = os.path.join(os.path.dirname(os.path.abspath(__file__)), "prompt.md")
DEEP_MODEL = os.environ.get("MOONSHOT_MODEL", "moonshot-v1-32k")


def load_prompt_template(path=PROMPT_PATH):
    with open(path, encoding="utf-8") as f:
        return f.read()


def build_prompt(template, article, article_text):
    return (template
            .replace("{source}", article["source"])
            .replace("{title}", article["title"])
            .replace("{url}", article["url"])
            .replace("{content}", article_text))


def parse_interpretation(text):
    m = re.search(r"\{.*\}", text, re.DOTALL)
    if not m:
        raise ValueError("no JSON object in model output")
    data = json.loads(m.group(0))
    for key in ("relevant", "importance", "one_liner", "value",
                "comparison", "credibility"):
        if key not in data:
            raise ValueError(f"missing key: {key}")
    return data
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_interpret.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add interpret.py tests/test_interpret.py
git commit -m "feat: prompt 构建与解读 JSON 解析"
```

---

### Task 9: 双引擎调用与 interpret()

**Files:**
- Modify: `interpret.py`
- Test: `tests/test_interpret.py`

- [ ] **Step 1: 写失败测试**

`tests/test_interpret.py` 追加：
```python
GOOD = ('{"relevant": true, "importance": 3, "one_liner": "a",'
        ' "value": "b", "comparison": "c", "credibility": "official"}')


def test_interpret_relevant():
    article = {"source": "S", "title": "T", "url": "U"}
    data = interpret.interpret(article, "text", runner=lambda p: GOOD)
    assert data["importance"] == 3


def test_interpret_irrelevant():
    article = {"source": "S", "title": "T", "url": "U"}
    out = interpret.interpret(
        article, "text",
        runner=lambda p: GOOD.replace('"relevant": true', '"relevant": false'))
    assert out is None


def test_run_kimi_cli():
    from unittest.mock import patch, MagicMock
    fake = MagicMock(stdout="model output", returncode=0)
    with patch("interpret.subprocess.run", return_value=fake) as run:
        out = interpret.run_kimi_cli("hello")
    assert out == "model output"
    assert run.call_args[0][0][:2] == ["kimi", "-p"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_interpret.py -v`
Expected: FAIL，无 `interpret` / `run_kimi_cli`

- [ ] **Step 3: 实现**

`interpret.py` 追加：
```python
def run_kimi_cli(prompt, timeout=600):
    result = subprocess.run(["kimi", "-p", prompt],
                            capture_output=True, text=True, timeout=timeout)
    result.check_returncode()
    return result.stdout


def call_moonshot(prompt, timeout=120):
    api_key = os.environ["MOONSHOT_API_KEY"]
    resp = requests.post(
        "https://api.moonshot.cn/v1/chat/completions",
        headers={"Authorization": f"Bearer {api_key}"},
        json={"model": DEEP_MODEL,
              "messages": [{"role": "user", "content": prompt}],
              "temperature": 0.3},
        timeout=timeout)
    resp.raise_for_status()
    return resp.json()["choices"][0]["message"]["content"]


def interpret(article, article_text, deep=False, runner=None):
    """返回解读 dict；文章与发布无关时返回 None。"""
    prompt = build_prompt(load_prompt_template(), article, article_text)
    if runner is None:
        runner = call_moonshot if deep else run_kimi_cli
    data = parse_interpretation(runner(prompt))
    return data if data["relevant"] else None
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_interpret.py -v`
Expected: 6 passed

- [ ] **Step 5: Commit**

```bash
git add interpret.py tests/test_interpret.py
git commit -m "feat: kimi CLI / Moonshot 双引擎解读"
```

---

### Task 10: 飞书卡片构建

**Files:**
- Create: `push_lark.py`
- Test: `tests/test_push_lark.py`

- [ ] **Step 1: 写失败测试**

`tests/test_push_lark.py`:
```python
import push_lark


ARTICLE = {"source": "OpenAI", "title": "GPT-X", "url": "https://x.com/a"}
INTERP = {"relevant": True, "importance": 5, "one_liner": "新模型",
          "value": "更好用", "comparison": "领先", "credibility": "official"}


def test_build_card_important_is_red():
    card = push_lark.build_card(ARTICLE, INTERP)
    assert card["msg_type"] == "interactive"
    assert card["card"]["header"]["template"] == "red"
    assert "OpenAI" in card["card"]["header"]["title"]["content"]


def test_build_card_normal_is_blue():
    interp = dict(INTERP, importance=2)
    card = push_lark.build_card(ARTICLE, interp)
    assert card["card"]["header"]["template"] == "blue"


def test_digest_card():
    items = [(ARTICLE, INTERP), (dict(ARTICLE, title="第二篇"), INTERP)]
    card = push_lark.build_digest_card("OpenAI", items)
    assert "连发 2 篇" in card["card"]["header"]["title"]["content"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_push_lark.py -v`
Expected: FAIL，`ModuleNotFoundError`

- [ ] **Step 3: 实现**

`push_lark.py`:
```python
import os
import time

import requests


def stars(n):
    return "★" * n + "☆" * (5 - n)


def build_card(article, interp):
    color = "red" if interp["importance"] >= 4 else "blue"
    return {
        "msg_type": "interactive",
        "card": {
            "header": {
                "template": color,
                "title": {"tag": "plain_text",
                          "content": f"{article['source']}｜{article['title']}"},
            },
            "elements": [
                {"tag": "markdown",
                 "content": f"**{stars(interp['importance'])}**\n\n**一句话**：{interp['one_liner']}"},
                {"tag": "markdown",
                 "content": f"**对你有什么用**：{interp['value']}"},
                {"tag": "markdown",
                 "content": f"**和别家比**：{interp['comparison']}"},
                {"tag": "markdown",
                 "content": f"可信度：{interp['credibility']}"},
                {"tag": "action",
                 "actions": [{"tag": "button",
                              "text": {"tag": "plain_text", "content": "查看原文"},
                              "url": article["url"],
                              "type": "primary"}]},
            ],
        },
    }


def build_digest_card(source_name, items):
    lines = [f"- [{a['title']}]({a['url']}) {stars(i['importance'])} {i['one_liner']}"
             for a, i in items]
    return {
        "msg_type": "interactive",
        "card": {
            "header": {"template": "blue",
                       "title": {"tag": "plain_text",
                                 "content": f"{source_name}｜连发 {len(items)} 篇"}},
            "elements": [{"tag": "markdown", "content": "\n".join(lines)}],
        },
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_push_lark.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add push_lark.py tests/test_push_lark.py
git commit -m "feat: 飞书卡片构建"
```

---

### Task 11: 推送与重试

**Files:**
- Modify: `push_lark.py`
- Test: `tests/test_push_lark.py`

- [ ] **Step 1: 写失败测试**

`tests/test_push_lark.py` 追加：
```python
from unittest.mock import patch, MagicMock


def test_push_success():
    resp = MagicMock(ok=True)
    resp.json.return_value = {"code": 0}
    with patch("push_lark.requests.post", return_value=resp):
        assert push_lark.push({"msg_type": "interactive", "card": {}},
                              webhook="https://hook.example/x")


def test_push_retries_then_fails():
    with patch("push_lark.requests.post", side_effect=Exception("boom")), \
         patch("push_lark.time.sleep"):
        assert not push_lark.push({}, webhook="https://hook.example/x")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_push_lark.py -v`
Expected: FAIL，无 `push`

- [ ] **Step 3: 实现**

`push_lark.py` 追加：
```python
def push(card, webhook=None, retries=3):
    webhook = webhook or os.environ["LARK_WEBHOOK_URL"]
    for attempt in range(retries):
        try:
            resp = requests.post(webhook, json=card, timeout=15)
            if resp.ok and resp.json().get("code", 0) == 0:
                return True
        except Exception as e:
            print(f"[push] attempt {attempt + 1} failed: {e}")
        time.sleep(2 ** attempt)
    return False
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_push_lark.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add push_lark.py tests/test_push_lark.py
git commit -m "feat: webhook 推送与指数退避重试"
```

---

### Task 12: 静态站生成

**Files:**
- Create: `template.html`
- Create: `build_site.py`
- Test: `tests/test_build_site.py`

- [ ] **Step 1: 写失败测试**

`tests/test_build_site.py`:
```python
import json
import sqlite3

import build_site
import fetch


def seed(db_path):
    conn = fetch.init_db(db_path)
    conn.execute(
        "INSERT INTO articles (source, url, title, title_hash, status,"
        " importance, interpretation, pushed_at) VALUES (?,?,?,?,?,?,?,"
        " datetime('now'))",
        ("OpenAI", "https://x.com/a", "GPT-X", "h", "pushed", 4,
         json.dumps({"one_liner": "新模型", "value": "更好用",
                     "comparison": "领先", "credibility": "official"},
                    ensure_ascii=False)))
    conn.commit()
    conn.close()


def test_build(tmp_path):
    db = str(tmp_path / "t.db")
    seed(db)
    out = str(tmp_path / "site")
    build_site.build(db, out)
    with open(f"{out}/index.html", encoding="utf-8") as f:
        page = f.read()
    assert "GPT-X" in page and "新模型" in page
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_build_site.py -v`
Expected: FAIL，`ModuleNotFoundError`

- [ ] **Step 3: 写 template.html**

`template.html`:
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>AI Radar</title>
<style>
  body { max-width: 760px; margin: 2rem auto; padding: 0 1rem;
         font-family: -apple-system, "PingFang SC", sans-serif; }
  nav a { margin-right: .8rem; }
  .card { border: 1px solid #ddd; border-radius: 8px;
          padding: 1rem; margin: 1rem 0; }
  .stars { color: #e6a23c; margin: 0; }
</style>
</head>
<body>
<nav>{nav}</nav>
{content}
</body>
</html>
```

- [ ] **Step 4: 实现 build_site.py**

`build_site.py`:
```python
import html
import json
import os
import sqlite3
from collections import defaultdict

TEMPLATE_PATH = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                             "template.html")


def load_pushed(db_path="data/radar.db"):
    conn = sqlite3.connect(db_path)
    rows = conn.execute(
        "SELECT source, url, title, importance, interpretation, fetched_at"
        " FROM articles WHERE status = 'pushed'"
        " ORDER BY fetched_at DESC").fetchall()
    conn.close()
    items = []
    for source, url, title, importance, interp_json, fetched_at in rows:
        items.append({"source": source, "url": url, "title": title,
                      "importance": importance,
                      "interp": json.loads(interp_json),
                      "date": (fetched_at or "")[:10]})
    return items


def render_card(item):
    i = item["interp"]
    return (
        f'<article class="card">\n'
        f"  <h2>{html.escape(item['source'])}｜{html.escape(item['title'])}</h2>\n"
        f"  <p class=\"stars\">{'★' * item['importance']}</p>\n"
        f"  <p><b>一句话</b>：{html.escape(i['one_liner'])}</p>\n"
        f"  <p><b>对你有什么用</b>：{html.escape(i['value'])}</p>\n"
        f"  <p><b>和别家比</b>：{html.escape(i['comparison'])}</p>\n"
        f"  <p><a href=\"{html.escape(item['url'])}\">查看原文</a>"
        f" · {item['date']} · 可信度：{html.escape(i['credibility'])}</p>\n"
        f"</article>")


def render_page(template, nav, content):
    return template.replace("{nav}", nav).replace("{content}", content)


def build(db_path="data/radar.db", out_dir="data/site"):
    items = load_pushed(db_path)
    with open(TEMPLATE_PATH, encoding="utf-8") as f:
        template = f.read()
    by_source = defaultdict(list)
    for it in items:
        by_source[it["source"]].append(it)
    nav = " ".join(f'<a href="#{html.escape(s)}">{html.escape(s)}</a>'
                   for s in sorted(by_source))
    sections = []
    for source in sorted(by_source):
        cards = "\n".join(render_card(it) for it in by_source[source])
        sections.append(f'<section id="{html.escape(source)}">'
                        f"<h1>{html.escape(source)}</h1>\n{cards}</section>")
    os.makedirs(out_dir, exist_ok=True)
    with open(os.path.join(out_dir, "index.html"), "w", encoding="utf-8") as f:
        f.write(render_page(template, nav, "\n".join(sections)))
    # 按月归档
    by_month = defaultdict(list)
    for it in items:
        by_month[it["date"][:7]].append(it)
    arch_dir = os.path.join(out_dir, "archive")
    os.makedirs(arch_dir, exist_ok=True)
    for month, month_items in by_month.items():
        cards = "\n".join(render_card(it) for it in month_items)
        with open(os.path.join(arch_dir, f"{month}.html"), "w",
                  encoding="utf-8") as f:
            f.write(render_page(template, "", cards))
```

- [ ] **Step 5: 跑测试确认通过**

Run: `.venv/bin/pytest tests/test_build_site.py -v`
Expected: 1 passed

- [ ] **Step 6: Commit**

```bash
git add template.html build_site.py tests/test_build_site.py
git commit -m "feat: 静态站生成（首页 + 按月归档）"
```

---

### Task 13: main.py 管道与 weekly 汇总

**Files:**
- Create: `main.py`
- Test: `tests/test_main.py`

- [ ] **Step 1: 写失败测试**

`tests/test_main.py`:
```python
import json
from unittest.mock import patch

import main


GOOD = {"relevant": True, "importance": 3, "one_liner": "a",
        "value": "b", "comparison": "c", "credibility": "official"}


def test_run_end_to_end(tmp_path):
    db = str(tmp_path / "t.db")
    src = str(tmp_path / "s.yaml")
    with open(src, "w") as f:
        f.write("sources: []\n")
    with patch("main.fetch.collect", return_value=[1]), \
         patch("main.fetch.init_db") as init_db, \
         patch("main.fetch.fetch_article_text", return_value="text"), \
         patch("main.interpret.interpret", return_value=GOOD), \
         patch("main.push_lark.build_card", return_value={}), \
         patch("main.push_lark.push", return_value=True), \
         patch("main.build_site.build"):
        import sqlite3
        conn = sqlite3.connect(db)
        conn.executescript(main.fetch.SCHEMA)
        conn.execute(
            "INSERT INTO articles (id, source, url, title, title_hash)"
            " VALUES (1, 'S', 'https://x.com/a', 'T', 'h')")
        conn.commit()
        init_db.return_value = conn
        main.run(db, src)
        status = conn.execute(
            "SELECT status FROM articles WHERE id = 1").fetchone()[0]
        conn.close()
    assert status == "pushed"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `.venv/bin/pytest tests/test_main.py -v`
Expected: FAIL，`ModuleNotFoundError`

- [ ] **Step 3: 实现 main.py**

`main.py`:
```python
import json
import sys
from collections import defaultdict

import build_site
import fetch
import interpret
import push_lark


def run(db_path="data/radar.db", sources_path="sources.yaml"):
    new_ids = fetch.collect(db_path, sources_path)
    conn = fetch.init_db(db_path)
    done = []
    for row_id in new_ids:
        row = conn.execute(
            "SELECT id, source, url, title FROM articles WHERE id = ?",
            (row_id,)).fetchone()
        if not row:
            continue
        article = {"id": row[0], "source": row[1], "url": row[2],
                   "title": row[3]}
        try:
            text = fetch.fetch_article_text(article["url"])
            data = interpret.interpret(article, text)
        except Exception as e:
            print(f"[interpret] {article['url']} failed: {e}")
            conn.execute("UPDATE articles SET status = 'failed' WHERE id = ?",
                         (row_id,))
            conn.commit()
            continue
        if data is None:
            conn.execute("UPDATE articles SET status = 'skipped' WHERE id = ?",
                         (row_id,))
            conn.commit()
            continue
        if data["importance"] >= 4:
            try:
                deep = interpret.interpret(article, text, deep=True)
                if deep:
                    data = deep
            except Exception as e:
                print(f"[deep] 保留普通版: {e}")
        conn.execute(
            "UPDATE articles SET status = 'interpreted', importance = ?,"
            " interpretation = ? WHERE id = ?",
            (data["importance"], json.dumps(data, ensure_ascii=False), row_id))
        conn.commit()
        done.append((article, data))
    by_source = defaultdict(list)
    for article, data in done:
        by_source[article["source"]].append((article, data))
    for source, items in by_source.items():
        card = (push_lark.build_card(*items[0]) if len(items) == 1
                else push_lark.build_digest_card(source, items))
        if push_lark.push(card):
            for article, _ in items:
                conn.execute(
                    "UPDATE articles SET status = 'pushed',"
                    " pushed_at = datetime('now') WHERE id = ?",
                    (article["id"],))
            conn.commit()
    conn.close()
    build_site.build(db_path)


def weekly(db_path="data/radar.db"):
    conn = fetch.init_db(db_path)
    rows = conn.execute(
        "SELECT source, url, title, importance FROM articles"
        " WHERE status = 'pushed'"
        " AND pushed_at >= datetime('now', '-7 days')"
        " ORDER BY importance DESC").fetchall()
    conn.close()
    if not rows:
        print("[weekly] 本周无推送记录")
        return
    by_source = defaultdict(list)
    for source, url, title, importance in rows:
        by_source[source].append((title, url, importance))
    lines = []
    for source in sorted(by_source):
        lines.append(f"**{source}**")
        for title, url, importance in by_source[source]:
            lines.append(f"- [{title}]({url}) {'★' * importance}")
    best = rows[0]
    lines.append(f"\n**本周最值得关注**：[{best[2]}]({best[1]})")
    card = {
        "msg_type": "interactive",
        "card": {
            "header": {"template": "orange",
                       "title": {"tag": "plain_text",
                                 "content": "AI Radar 本周汇总"}},
            "elements": [{"tag": "markdown", "content": "\n".join(lines)}],
        },
    }
    push_lark.push(card)


if __name__ == "__main__":
    if "--weekly" in sys.argv:
        weekly()
    else:
        run()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `.venv/bin/pytest tests/ -v`
Expected: 全部 passed（含 Task 13 新增）

- [ ] **Step 5: Commit**

```bash
git add main.py tests/test_main.py
git commit -m "feat: 主管道与 weekly 汇总"
```

---

### Task 14: launchd 调度与 README

**Files:**
- Create: `com.airadar.plist`
- Create: `com.airadar.weekly.plist`
- Create: `README.md`

- [ ] **Step 1: 写两个 plist（日常 + 周日汇总）**

`com.airadar.plist`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>com.airadar</string>
  <key>ProgramArguments</key>
  <array>
    <string>/Users/bytedance/ai-radar/.venv/bin/python</string>
    <string>/Users/bytedance/ai-radar/main.py</string>
  </array>
  <key>WorkingDirectory</key><string>/Users/bytedance/ai-radar</string>
  <key>StartCalendarInterval</key>
  <array>
    <dict><key>Hour</key><integer>8</integer><key>Minute</key><integer>0</integer></dict>
    <dict><key>Hour</key><integer>20</integer><key>Minute</key><integer>0</integer></dict>
  </array>
  <key>StandardOutPath</key><string>/Users/bytedance/ai-radar/data/run.log</string>
  <key>StandardErrorPath</key><string>/Users/bytedance/ai-radar/data/run.log</string>
</dict>
</plist>
```

`com.airadar.weekly.plist`:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>com.airadar.weekly</string>
  <key>ProgramArguments</key>
  <array>
    <string>/Users/bytedance/ai-radar/.venv/bin/python</string>
    <string>/Users/bytedance/ai-radar/main.py</string>
    <string>--weekly</string>
  </array>
  <key>WorkingDirectory</key><string>/Users/bytedance/ai-radar</string>
  <key>StartCalendarInterval</key>
  <dict><key>Weekday</key><integer>0</integer><key>Hour</key><integer>9</integer><key>Minute</key><integer>0</integer></dict>
  <key>StandardOutPath</key><string>/Users/bytedance/ai-radar/data/weekly.log</string>
  <key>StandardErrorPath</key><string>/Users/bytedance/ai-radar/data/weekly.log</string>
</dict>
</plist>
```

- [ ] **Step 2: 写 README.md**

`README.md`:
```markdown
# AI Radar

监控主流 AI 公司发布博客，kimi 自动大白话解读，推送飞书，生成静态站。

## 用法

```bash
python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
export LARK_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxx
export MOONSHOT_API_KEY=sk-xxx   # 可选，importance≥4 深度解读用
.venv/bin/python main.py          # 跑一轮：抓取→解读→推送→建站
.venv/bin/python main.py --weekly # 本周汇总
```

## 调度

```bash
mkdir -p data
cp com.airadar*.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.airadar.plist
launchctl load ~/Library/LaunchAgents/com.airadar.weekly.plist
```

## 结构

- `sources.yaml` 监控源（实测后定稿；纯 JS 渲染无法抓取的源见 git 历史）
- `prompt.md` 解读 prompt（核心资产）
- `fetch.py` / `interpret.py` / `push_lark.py` / `build_site.py` / `main.py`
```

- [ ] **Step 3: 验证 plist 语法**

```bash
plutil -lint com.airadar.plist com.airadar.weekly.plist
```
Expected: 两个文件均 `OK`

- [ ] **Step 4: Commit**

```bash
git add com.airadar.plist com.airadar.weekly.plist README.md
git commit -m "chore: launchd 调度与 README"
git push
```

---

### Task 15: 端到端真实冒烟

**Files:** 无（运行验证）

- [ ] **Step 1: 配置 webhook**

在飞书群里加一个自定义机器人，把 webhook 地址 `export LARK_WEBHOOK_URL=...`。

- [ ] **Step 2: 真实跑一轮**

```bash
cd /Users/bytedance/ai-radar
.venv/bin/python main.py
```
Expected: 无 traceback；若抓到新文章，飞书收到卡片；`data/site/index.html` 生成。首次跑可能文章较多——如推送刷屏，先临时把 `sources.yaml` 减到 1 个源验证。

- [ ] **Step 3: 检查解读质量**

人工读 2-3 条解读：非技术人员能否看懂？"对你有什么用"是否具体？不满意则改 `prompt.md` 重跑（把对应行 `status` 改回 `new` 并删掉 `interpretation` 即可重解读）：
```bash
sqlite3 data/radar.db "UPDATE articles SET status='new', interpretation=NULL WHERE id=<id>;"
```

- [ ] **Step 4: 装载 launchd**

```bash
mkdir -p data
cp com.airadar*.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.airadar.plist
launchctl load ~/Library/LaunchAgents/com.airadar.weekly.plist
launchctl list | grep airadar
```
Expected: 两个 job 列出

- [ ] **Step 5: 最终提交**

```bash
git add -A
git commit -m "chore: 冒烟通过，上线调度" || true
git push
```
